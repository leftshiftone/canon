package canon.model

class DatePicker(id: String,
                 `class`: String,
                 val name: String,
                 val source: String,
                 val size: Int) : AbstractRenderable(id, `class`)
