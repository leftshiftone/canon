package canon.model

class ReelValue(id: String,
                         `class`: String,
                         val value: String,
                         val valueType: String) : AbstractRenderable(id, `class`)
